## Exercises

**Exercise 1**
Choose the correct indefinite article (`un`, `una`, `unos`, `unas`) to complete each phrase based on the noun it describes.

a) \_\_\_ libro interesante (an interesting book)
b) \_\_\_ sillas rojas (some red chairs)
c) \_\_\_ problema difícil (a difficult problem)
d) \_\_\_ amigas simpáticas (some nice friends)

**Exercise 2**
A friend sends you a text message with a shopping list, but they use the wrong indefinite articles. Correct the article in each line and explain *why* the original was incorrect.

*Original List:*
- un manzanas
- una bolígrafos
- unos pluma

**Exercise 3**
Imagine you see a single cat in the street and tell your friend, "Mira, **un** gato." Later, you see a specific dog that you both know, whose name is Fido, and you say, "Mira, **el** perro." Explain the difference in your choice of article (`un` vs. `el`) based on the context.

**Exercise 4**
You are in a classroom. Your teacher says, "Necesito **unos** voluntarios para un proyecto." (I need **some** volunteers for a project). Why is it more appropriate for the teacher to use `unos` (indefinite) instead of `los` (definite) in this situation?

**Exercise 5**
Using the personal pronoun `Yo` (I), write two separate sentences describing things you have. One sentence must use a singular indefinite article (`un` or `una`), and the other must use a plural indefinite article (`unos` or `unas`).

**Exercise 6**
Your friend is describing their family in Spanish but is new to the language. They say: "Tengo **un** hermana y **un** hermanos." This sentence has two errors related to articles and noun agreement. Rewrite the sentence correctly and explain the two changes you made.

---

## Answer Key

**Answer 1**
a) **un** libro interesante
b) **unas** sillas rojas
c) **un** problema difícil
d) **unas** amigas simpáticas

**Reasoning:**
- `libro` is a masculine singular noun, so it requires `un`.
- `sillas` is a feminine plural noun, so it requires `unas`.
- `problema` is a masculine singular noun (even though it ends in -a), so it requires `un`.
- `amigas` is a feminine plural noun, so it requires `unas`.

**Answer 2**
The corrected list is:
- **una** manzana (or **unas** manzanas)
- **unos** bolígrafos
- **una** pluma

**Reasoning:**
- "un manzanas": `manzanas` is feminine and plural, so the article must be `unas`. If the friend meant only one apple, it would be `una manzana`. `un` is incorrect because it is masculine singular.
- "una bolígrafos": `bolígrafos` is masculine and plural, so the article must be `unos`. `una` is incorrect because it is feminine singular.
- "unos pluma": `pluma` is feminine and singular, so the article must be `una`. `unos` is incorrect because it is masculine and plural.

**Answer 3**
**Reasoning:**
You use `un gato` because you are referring to **any** cat, a non-specific one that neither you nor your friend has identified before. The indefinite article `un` is used for a member of a general group ('a cat').

You use `el perro` because you are referring to Fido, a **specific** dog that you both already know. The definite article `el` points to a particular, identifiable noun ('the dog'). The choice reflects the difference between a non-specific reference and a specific one.

**Answer 4**
**Reasoning:**
The teacher uses `unos` because they are not calling for specific, pre-determined volunteers. They are asking for **any** volunteers from the general group of students. Using `los voluntarios` would imply that there is a specific, known group of volunteers that the teacher is referring to, which is not the case. The indefinite article `unos` correctly conveys the meaning of "some" or "any" non-specific people.

**Answer 5**
*Sample answers are provided, but any grammatically correct sentences are acceptable.*

*Sentence 1 (singular):* "Yo tengo **un** coche azul." (I have a blue car.)
*Sentence 2 (plural):* "Yo tengo **unas** plantas en mi casa." (I have some plants in my house.)

**Reasoning:**
The first sentence uses `un` because `coche` is a masculine singular noun. The second sentence uses `unas` because `plantas` is a feminine plural noun. The exercise demonstrates the ability to form a complete thought by integrating a subject (`Yo`), a verb (`tengo`), and a noun phrase that correctly uses an indefinite article matching the noun's gender and number.

**Answer 6**
*Corrected Sentence:* "Tengo **una** hermana y **unos** hermanos."

**Reasoning:**
1.  **Change 1:** `un hermana` was changed to `una hermana`. The noun `hermana` (sister) is feminine and singular, so it requires the feminine singular article `una`.
2.  **Change 2:** `un hermanos` was changed to `unos hermanos`. The noun `hermanos` (brothers, or brothers and sisters) is masculine and plural. It requires the masculine plural article `unos`. The original `un` was incorrect in both gender (masculine was correct, but by coincidence) and number (it was singular).